[project]
name = "python-demo"
version = "0.1.0"
description = "Minimal example project for vibe-coding-starter-kit"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
testpaths = ["tests"]
